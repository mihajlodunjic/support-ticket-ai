spring:
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/helpdesk}
    username: ${SPRING_DATASOURCE_USERNAME:helpdesk}
    password: ${SPRING_DATASOURCE_PASSWORD:helpdesk}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    enabled: true
    locations: classpath:db/migration

app:
  security:
    jwt-secret: ${APP_SECURITY_JWT_SECRET}
    token-validity-seconds: ${APP_SECURITY_TOKEN_VALIDITY_SECONDS:3600}
    admin:
      name: ${APP_SECURITY_ADMIN_NAME:}
      email: ${APP_SECURITY_ADMIN_EMAIL:}
      password: ${APP_SECURITY_ADMIN_PASSWORD:}
